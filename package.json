{
  "name": "wirays-coverage-app",
  "version": "1.0.0",
  "description": "WiRays Coverage App — iOS app via Capacitor",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
